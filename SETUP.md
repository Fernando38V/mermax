# DEPENDECIAS

## Actualizar requirements | Este se pone cada vez que se intala una nueva dependencia | EJECUTAR DENTRO DEL ENTORNO :

>    pip freeze > requirements.txt

## Instalar dependencias | Este es para instalar todo lo que esta en el archivo requirements.txt | EJECUTAR DENTRO DEL ENTORNO :

>    pip install -r requirements.txt
